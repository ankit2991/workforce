import swaggerJsdoc from 'swagger-jsdoc';

const options: swaggerJsdoc.Options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'WorkPay API - Mobile-First Workforce Wallet, Gaming & Shop',
      version: '1.0.0',
      description: 'Production-ready REST API for employee digital wallet, salary advance, instant withdrawals, gaming wallet, and company shop.',
    },
    servers: [
      {
        url: 'http://localhost:5001/api/v1',
        description: 'Local Development Server',
      },
    ],
    components: {
      securitySchemes: {
        bearerAuth: {
          type: 'http',
          scheme: 'bearer',
          bearerFormat: 'JWT',
        },
      },
    },
    security: [
      {
        bearerAuth: [],
      },
    ],
  },
  apis: ['./src/modules/**/*.routes.ts', './src/modules/**/*.controller.ts'],
};

export const swaggerSpec = swaggerJsdoc(options);
